# Redis configuration để tránh warnings
# Disable IPv6 binding
bind 0.0.0.0

# Disable protected mode (cho Docker)
protected-mode no

# Memory overcommit warning - không thể fix trong container, nhưng không ảnh hưởng
# Cần fix ở host level: sysctl vm.overcommit_memory=1

